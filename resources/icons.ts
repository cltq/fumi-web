// Custom icon library for Once UI
// Add your custom icons here using react-icons or other icon libraries

import type { IconType } from "react-icons";

export const iconLibrary: Record<string, IconType> = {
  // Add custom icons here as needed
  // Example: home: HiHome,
};
